var x = 100;
var y = 100;


function setup() {
    createCanvas (400,400);
}

function draw () {
    background(0);
    circle(width-mouseX, height-mouseX, 30);
    fill (245, 123, 66); 

    // if (mouseX = x,y) {
    //     x = -x; //Flip direction
    // } 
    // if (mouseY = x,y) {
    //     y = -y; 
    // }

}

//orange circle
// x,y of circle
//set circle opposite of where mouse is

